DB_HOST=localhost
DB_PORT=5432
DB_USER=pmsystem
DB_PASSWORD=pmsystem
DB_NAME=pmsystem
PORT=3001
JWT_SECRET=change-me-in-production

# Modo instalação local (desktop): defina DB_TYPE=sqlite para usar um arquivo
# SQLite em vez de Postgres — não exige Docker nem servidor de banco.
# DB_TYPE=sqlite
# DB_SQLITE_PATH=./data/pmsystem.sqlite
# UPLOADS_DIR=./uploads
